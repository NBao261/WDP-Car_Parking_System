feat/fix: update pricing and facility UI behaviors

Pricing Management:
- Fixed PricingFormModal conditional rendering: hide 'Cấu hình mức giá' and 'Phụ phí' sections entirely until a fee type is selected.
- Removed red background from input error state in PricingFormModal for cleaner validation UI.
- Updated default values and placeholders in PricingFormModal to allow empty inputs to display placeholders instead of 0.
- Improved PricingDetailModal layout:
  - Changed 'Tòa nhà áp dụng' and 'Loại xe & Loại giá' from 2-column grid to full width row-by-row layout.
  - Split 'Loại xe' and 'Loại giá' into two separate columns.
  - Wrapped facility name and address inside a styled background (w-fit bg-gray-50/80 p-3.5) and increased address font size to text-sm.
  - Changed address text color to black (text-gray-800).

Facility & Floor Management (View-only for inactive facilities):
- FacilityFloorsView: Hidden 'Thêm Tầng' button if the selected facility is inactive.
- FloorCard: Completely hide the floor action menu (3 dots) when facility is inactive.
- FloorGrid: Hidden the 'Thêm Tầng' button in the empty state placeholder if facility is inactive.
- SlotMappingEditorView: Hidden 'Tạo Slot' button when facility is inactive.
- SlotMappingEditorView: Prevented clicking to edit slots if facility is inactive and show proper toast error message.
