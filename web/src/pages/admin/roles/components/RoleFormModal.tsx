// RoleFormModal đã được loại bỏ theo SRS mới:
// Hệ thống chỉ có 4 System Roles cố định (admin, manager, staff, driver).
// Tính năng Tạo / Chỉnh sửa Role không còn trong scope.
// Để quản lý quyền hạn, dùng PermissionMatrixModal.

export {};
