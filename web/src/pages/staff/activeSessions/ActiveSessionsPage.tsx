import { UnderDevelopment } from '../../../components/UnderDevelopment';

export default function ActiveSessionsPage() {
  return (
    <UnderDevelopment
      featureName="Active Sessions"
      description="Quản lý danh sách phương tiện (xe máy, ô tô, xe đạp, xe tải)"
    />
  );
}